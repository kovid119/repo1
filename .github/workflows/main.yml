on:
  push:
    branches:
      - dev
  workflow_dispatch:
  
jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest
    
    steps:
    #- name: install ssh keys
      # check this thread to understand why its needed:
      # https://stackoverflow.com/a/70447517
     # run: |
        #install -m 600 -D /dev/null ~/.ssh/id_rsa
        #echo "${{ secrets.SSH_PRIVATE_KEY1 }}" > /home/eirgt/.ssh/id_rsa
        #ssh-keyscan -H ${{ secrets.SSH_IP2 }} > /home/eirgt/.ssh/known_hosts
    - name: connect and pull
      run: ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_IP2 }} "cd ${{ secrets.WORK_DIR }} && hostname -f && ifconfig"
    #- name: cleanup
     # run: rm -rf ~/.ssh
-
